If statements
=============

Based on a boolean opperator, then executes a certain branch of code

.. code-block::
	:linenos:

	if (a > b) {
		println("Greater");
	} else {
		println("Less than or equal to")
	}